# -----------------------------------------------------------------------------
# Visual Studio (VS) and Windows Files
# -----------------------------------------------------------------------------
## Build results and temporary files
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
*.suo
*.user
*.userosscache
*.sln.docstates
*.vshost.exe
*.vshost.exe.config
*.bak
*.ncb
*.sdf
*.tlb
*.tlh
*.log
*.vspscc
*.vssscc
# Resharper
_ReSharper*/
*.DotSettings.user

## Visual Studio 2017+ cache/options directory
# Exclude the entire .vs folder except for the `settings.json` and `launch.json`
# if you want to share specific debugger/workspace settings (often NOT recommended).
# Uncomment the block below to ignore ALL of .vs/
.vs/

# Uncomment these if you only want to ignore the cache files but keep other config
# .vs/
# !.vs/ProjectSettings.json
# !.vs/VSWorkspaceSettings.json

# -----------------------------------------------------------------------------
# Visual Studio Code (VS Code)
# -----------------------------------------------------------------------------
## User-specific settings and workspace files
.vscode/
# Keep files you want to share, typically only `settings.json` and `extensions.json`
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# -----------------------------------------------------------------------------
# JavaScript, Node.js, and NPM
# -----------------------------------------------------------------------------
## Dependency directories
/node_modules
/jspm_packages

## NPM specific files
npm-debug.log*
.npm/

## Yarn/pnpm/etc.
.yarn/
.pnp.cjs
.pnp.loader.cjs

## Testing and coverage
/coverage
/test/coverage
.nyc_output

## TypeScript cache and generated files
*.tsbuildinfo

## Environment variables
.env
.env.development
.env.production
.env.local
.env.*.local
.env.test

## Bundler/Build tool output directories
/dist
/build
/out
.cache
.parcel-cache
# Next.js
.next/
# Gatsby
.cache/
/public # Only if generated by build process, e.g., Next.js `out` folder is often generated.

# -----------------------------------------------------------------------------
# Operating System Files
# -----------------------------------------------------------------------------
.DS_Store # macOS
Thumbs.db # Windows